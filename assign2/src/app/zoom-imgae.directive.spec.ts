import { ZoomImgaeDirective } from './zoom-imgae.directive';

describe('ZoomImgaeDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomImgaeDirective();
    expect(directive).toBeTruthy();
  });
});
